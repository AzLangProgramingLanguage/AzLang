sabit c = "asda".böyüt().tərs()
Çap(c)

funksiya faktorial(x: ədəd):ədəd
    əgər x == 0
        qaytar 1
    əks
        qaytar x * faktorial(x - 1)  //Burası transpile olunarken azlangEded tipinde transpile olunmalıdır.
Çap(faktorial(5))
